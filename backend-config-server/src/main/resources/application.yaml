server:
  port: 8081
spring:
  application:
    name: backend-config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/EnzoGulppi/TopEducation_MS
          search-paths: "config-data"